import{a as e}from"./chunk-FJ4PUOMA.js";import"./chunk-IFGU66OU.js";var t={meta:e,sections:[{id:"overview",title:"Overview",blocks:[{type:"markdown",value:"A **rate limiter** controls how many requests a client may make in a window of time, rejecting or delaying the excess. It is the front-line defense that keeps a single abusive client \u2014 or a buggy retry loop, or a DDoS \u2014 from overwhelming a service, and it enforces fair usage and paid quotas. Almost every public API (Stripe, GitHub, Twitter) sits behind one."},{type:"callout",variant:"info",title:"The big idea",body:'Conceptually trivial ("count requests, reject over the limit"), a rate limiter is hard for three reasons: (1) choosing an **algorithm** with the right burst/smoothness behavior; (2) making the counter **correct under concurrency** (atomic read-modify-write); and (3) making it **fast and consistent across many gateway nodes** without adding latency to every request.'},{type:"image",src:"assets/diagrams/rate-limiter-architecture.svg",alt:"Clients hit the API gateway; rate-limit middleware identifies the caller, checks an atomic counter in Redis via a Lua script, then allows (200, forward upstream) or rejects (429). A per-node local token cache reduces Redis round-trips.",caption:"Rate-limit middleware at the gateway checks an atomic Redis counter per identity+window, then allows (forward) or rejects (429)."}]},{id:"clarifying-questions",title:"Clarifying Questions",blocks:[{type:"markdown",value:"Rate limiter feels algorithm-heavy, but the first few minutes should be spent scoping *what* is being limited and *how precisely* \u2014 that choice decides which algorithm and topology are even relevant. Ask before reaching for token bucket vs sliding window."},{type:"table",caption:'Questions to ask, and reasonable assumptions if the interviewer says "you decide".',headers:["Question","Why it matters / sample assumption"],rows:[["What are we rate limiting \u2014 a single API, a whole gateway, or a specific expensive endpoint?","Assume a general-purpose limiter at the API gateway, configurable per-endpoint, since that is the most common and reusable design."],["What is the limiting key \u2014 API key, user ID, IP address, or a combination?","Assume per-API-key by default (most trustworthy identity), with IP-based limiting as a fallback for unauthenticated traffic."],["Do different clients/tiers need different limits (free vs paid)?","Yes \u2014 assume a rules engine maps scope + tier to a limit/window, hot-reloadable without a redeploy."],["How strict must accuracy be \u2014 is a small over-admission acceptable?","Assume a little slack is fine (e.g. \xB15%) in exchange for lower latency; tighten toward a centralized atomic counter only if the interviewer says quotas are billed."],["Should the limiter fail open or closed if its backing store is unavailable?","Default to **fail open** (allow traffic) so a store outage does not take down the whole API \u2014 call out fail-closed as the right choice for sensitive endpoints (e.g. payments)."],["Single data center or globally distributed clients/gateways?","Start single-region with one shared counter store; mention cross-region synchronization as a follow-up if asked to scale globally."]]},{type:"callout",variant:"tip",title:"State assumptions out loud",body:'Say explicitly: "I will assume per-API-key limiting, fail-open on store failure, and that a small amount of over-admission at the edges is acceptable." This tells the interviewer you understand the accuracy/latency/availability trade-off up front, instead of discovering it mid-design.'}]},{id:"functional-requirements",title:"Functional Requirements",blocks:[{type:"bestPractices",title:"In scope",practices:["**Limit requests** per client to N per time window (e.g. 100 req/min).","**Multiple scopes**: per API key, per user, per IP, per endpoint \u2014 and combinations.","**Tiered limits**: different quotas for free vs paid plans.","**Clear rejection**: return HTTP 429 with `Retry-After` and rate-limit headers.","**Distributed correctness**: a global limit honored across many gateway nodes.","**Low overhead**: add minimal latency to every allowed request."]},{type:"callout",variant:"note",title:"Out of scope (state explicitly)",body:"Full DDoS/WAF protection, bot detection, and billing/metering of usage. A rate limiter is one layer of defense; we focus on the counting algorithms and the distributed enforcement, not L3/L4 volumetric attack mitigation."}]},{id:"non-functional-requirements",title:"Non-Functional Requirements",blocks:[{type:"bestPractices",title:"Prioritizing the qualities",practices:["**Low latency**: the check sits in the hot path of every request \u2014 sub-millisecond is the target.","**High availability**: the limiter must not become a single point of failure for the whole API.","**Accuracy**: enforce limits closely (some slack is acceptable; large over-admission is not).","**Scalability**: handle the full request volume of the API it fronts.","**Configurability**: rules change without redeploying services."]},{type:"callout",variant:"tip",title:"The central tension: accuracy vs latency",body:"A perfectly accurate global counter requires a synchronous, strongly-consistent read-modify-write on every request \u2014 expensive. Most production limiters trade a little accuracy (allow slightly more than the limit at the edges) for big latency/availability wins via local caching and eventual sync. Know which way your use case leans."}]},{id:"where-to-enforce",title:"Where to Enforce",blocks:[{type:"markdown",value:"Rate limiting can live at several layers. The most common and effective place is the **API gateway / reverse proxy** \u2014 it sees all traffic, is centralized, and keeps the limiting logic out of every microservice."},{type:"featureComparison",caption:"Where to put the rate limiter.",columns:["Client-side","API gateway","Per-service middleware","Dedicated service"],rows:[{feature:"Trustworthy (cannot be bypassed)",values:[!1,!0,!0,!0]},{feature:"Centralized config",values:[!1,!0,!1,!0]},{feature:"Protects every service",values:[!1,!0,!1,!0]},{feature:"No extra network hop",values:[!0,!0,!0,!1]},{feature:"Common choice",values:[!1,!0,!1,!1]}]},{type:"callout",variant:"note",title:"Defense in depth",body:"Client-side limiting improves UX (avoid wasted calls) but is **never** trusted for protection \u2014 clients lie. The gateway is the enforcement point; a separate rate-limiter service (e.g. Envoy + a global rate-limit service) is used when many gateways/proxies must share one global counter."}]},{id:"algorithms",title:"Core Algorithms",blocks:[{type:"markdown",value:"Five algorithms dominate interviews and production APIs. They differ in how they treat **bursts**, how smoothly they shape traffic, and how much **memory/precision** they need. Pick by traffic shape and scale; always expose limits clearly via response headers so clients can backoff."},{type:"image",src:"assets/article-images/rate-limiter/01-intro.png",alt:"Illustration introducing rate limiting as protecting a service from too many requests",caption:"Rate limiting protects services from being overwhelmed by a single client. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"featureComparison",caption:"Rate-limiting algorithms compared.",columns:["Token Bucket","Leaky Bucket","Fixed Window","Sliding Window Log","Sliding Window Counter"],rows:[{feature:"Allows bursts",values:[!0,!1,!0,!1,"Partial"]},{feature:"Smooths output rate",values:[!1,!0,!1,!1,!1]},{feature:"Boundary spike problem",values:[!1,!1,!0,!1,!1]},{feature:"Memory per client",values:["O(1)","O(1)","O(1)","O(n)","O(1)"]},{feature:"Perfectly accurate",values:[!0,!0,!1,!0,"Approx"]}]},{type:"callout",variant:"tip",title:"The usual answer: token bucket (or sliding window counter)",body:"**Token bucket** is the most popular default \u2014 O(1), allows controlled bursts, intuitive. **Sliding window counter** is the go-to when you want fixed-window cheapness without the boundary spike. Leaky bucket is for **shaping** a smooth output rate. See also the shorter [Rate Limiter Pattern](/designs/rate-limiter-pattern)."}]},{id:"token-bucket",title:"Token Bucket",blocks:[{type:"markdown",value:"A bucket holds up to **B** tokens and refills at **r** tokens/second. Each request removes one token; if the bucket is empty, the request is rejected. Capacity **B** sets the maximum burst; refill rate **r** sets steady-state throughput. Store only token count and last refill timestamp per client."},{type:"image",src:"assets/article-images/rate-limiter/02-token-bucket.png",alt:"Token bucket diagram showing tokens filling a bucket and requests consuming tokens",caption:"Tokens refill at a fixed rate; requests consume tokens; empty bucket means reject. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"math",display:!0,tex:"tokens = \\min\\!\\big(B,\\; tokens + r \\cdot (now - last)\\big);\\quad \\text{allow if } tokens \\ge 1 \\Rightarrow tokens \\mathrel{-}= 1",caption:"Lazily refill based on elapsed time, cap at burst capacity B, then admit only if at least one token remains."},{type:"prosCons",title:"Token Bucket",pros:["Simple to implement and reason about","Allows bursts up to bucket capacity for short spikes"],cons:["Memory scales with number of identities if per-user","Does not guarantee a perfectly smooth request rate"]},{type:"image",src:"assets/article-images/rate-limiter/03-token-bucket-code.png",alt:"Code sketch of a token bucket rate limiter implementation",caption:"Reference implementation sketch. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"code",language:"java",filename:"TokenBucket.java",showLineNumbers:!0,code:`public final class TokenBucket {
  private final long capacity;
  private final double refillPerSec;
  private double tokens;
  private long lastNanos = System.nanoTime();

  public TokenBucket(long capacity, double refillPerSec) {
    this.capacity = capacity;
    this.refillPerSec = refillPerSec;
    this.tokens = capacity;
  }

  public synchronized boolean tryAcquire() {
    long now = System.nanoTime();
    tokens = Math.min(capacity,
        tokens + (now - lastNanos) / 1_000_000_000.0 * refillPerSec);
    lastNanos = now;
    if (tokens < 1) return false;
    tokens -= 1;
    return true;
  }
}`},{type:"code",language:"lua",filename:"token_bucket.lua",highlightLines:[8,9,10,14,15],code:`-- Atomic token bucket in Redis. KEYS[1]=bucket key
-- ARGV: rate, burst, now(ms), requested
local rate   = tonumber(ARGV[1])
local burst  = tonumber(ARGV[2])
local now    = tonumber(ARGV[3])
local need   = tonumber(ARGV[4])

local b = redis.call('HMGET', KEYS[1], 'tokens', 'ts')
local tokens = tonumber(b[1]) or burst
local ts     = tonumber(b[2]) or now

tokens = math.min(burst, tokens + (now - ts) / 1000 * rate)

local allowed = tokens >= need
if allowed then tokens = tokens - need end

redis.call('HMSET', KEYS[1], 'tokens', tokens, 'ts', now)
redis.call('PEXPIRE', KEYS[1], math.ceil(burst / rate * 1000))
return allowed and 1 or 0`},{type:"callout",variant:"warning",title:"Why a Lua script (atomicity)",body:"A naive GET-then-SET has a **race**: two nodes read the same token count, both admit. Redis runs a **Lua script atomically**, so read-refill-decrement-write is one indivisible operation."}]},{id:"leaky-bucket",title:"Leaky Bucket",blocks:[{type:"markdown",value:"Requests enter a bucket and **leak** (are processed) at a constant rate through a hole. If the bucket is full, new requests are discarded. Focus is on **smoothing** bursty traffic into a steady outflow."},{type:"image",src:"assets/article-images/rate-limiter/04-leaky-bucket.png",alt:"Leaky bucket diagram with requests entering the top and leaking at a constant rate",caption:"Constant leak rate; overflow when full. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"prosCons",title:"Leaky Bucket",pros:["Steady, predictable processing rate","Protects downstreams that need smooth inflow"],cons:["Does not absorb sudden bursts \u2014 excess is dropped","Slightly more complex than token bucket"]},{type:"image",src:"assets/article-images/rate-limiter/05-leaky-bucket-code.png",alt:"Code sketch of a leaky bucket rate limiter",caption:"Reference implementation sketch. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"code",language:"java",filename:"LeakyBucket.java",showLineNumbers:!0,code:`/** Queue-shaped leaky bucket: capacity slots, leakRate requests/sec. */
public final class LeakyBucket {
  private final int capacity;
  private final double leakPerSec;
  private double water;
  private long lastNanos = System.nanoTime();

  public LeakyBucket(int capacity, double leakPerSec) {
    this.capacity = capacity;
    this.leakPerSec = leakPerSec;
  }

  public synchronized boolean tryAcquire() {
    long now = System.nanoTime();
    water = Math.max(0, water - (now - lastNanos) / 1e9 * leakPerSec);
    lastNanos = now;
    if (water + 1 > capacity) return false;
    water += 1;
    return true;
  }
}`}]},{id:"window-algorithms",title:"Window Algorithms",blocks:[{type:"markdown",value:"Window-based counters are the other major family. Fixed windows are simple but have a famous **boundary spike**; sliding variants fix it at different cost."},{type:"markdown",value:`### Fixed Window Counter

Time is split into fixed windows (e.g. 1 minute). Each window has a counter; requests increment it; over the limit \u2192 deny until the next window.`},{type:"image",src:"assets/article-images/rate-limiter/06-fixed-window.png",alt:"Fixed window counter dividing time into intervals with per-window request counts",caption:"Fixed windows with independent counters. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"mermaid",caption:"Fixed-window boundary spike: 100 near the end of window 1 and 100 at the start of window 2 = 200 in ~1s.",definition:`flowchart LR
  subgraph W1["Window 1 (00:00-00:59)"]
    A["59.5s: 100 requests"]
  end
  subgraph W2["Window 2 (01:00-01:59)"]
    B["00.5s: 100 requests"]
  end
  A -->|"~1 second apart"| B
  B --> Spike["200 requests in ~1s"]`},{type:"callout",variant:"note",title:"Boundary nuance",body:"Windows need not align to calendar minutes \u2014 they can start at first request. Still, clock-aligned fixed windows remain common and can admit ~2\xD7 at edges. Token bucket bursts are intentional capacity; fixed-window edge spikes are usually unintended."},{type:"prosCons",title:"Fixed Window",pros:["Easy to implement","Clear per-window limits"],cons:["Boundary bursts can approach 2\xD7 the limit"]},{type:"image",src:"assets/article-images/rate-limiter/07-fixed-window-code.png",alt:"Code sketch of a fixed window counter",caption:"Reference implementation sketch. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"code",language:"java",filename:"FixedWindowCounter.java",code:`public final class FixedWindowCounter {
  private final int limit;
  private final long windowMs;
  private long windowStart;
  private int count;

  public FixedWindowCounter(int limit, Duration window) {
    this.limit = limit;
    this.windowMs = window.toMillis();
    this.windowStart = System.currentTimeMillis();
  }

  public synchronized boolean tryAcquire() {
    long now = System.currentTimeMillis();
    if (now - windowStart >= windowMs) {
      windowStart = now;
      count = 0;
    }
    if (count >= limit) return false;
    count++;
    return true;
  }
}`},{type:"markdown",value:`### Sliding Window Log

Keep a log of request timestamps. On each request, drop entries older than the window, count the rest; allow and append if under the limit.`},{type:"image",src:"assets/article-images/rate-limiter/08-sliding-window-log.png",alt:"Sliding window log of request timestamps within a rolling time window",caption:"Exact rolling window via timestamp log. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"prosCons",title:"Sliding Window Log",pros:["Very accurate \u2014 no window-edge artifact","Good for low-volume APIs"],cons:["Memory-intensive at high QPS","Must store and scan timestamps"]},{type:"image",src:"assets/article-images/rate-limiter/09-sliding-window-log-code.png",alt:"Code sketch of a sliding window log rate limiter",caption:"Reference implementation sketch. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"code",language:"java",filename:"SlidingWindowLog.java",code:`public final class SlidingWindowLog {
  private final int limit;
  private final long windowMs;
  private final Deque<Long> timestamps = new ArrayDeque<>();

  public SlidingWindowLog(int limit, Duration window) {
    this.limit = limit;
    this.windowMs = window.toMillis();
  }

  public synchronized boolean tryAcquire() {
    long now = System.currentTimeMillis();
    while (!timestamps.isEmpty() && now - timestamps.peekFirst() >= windowMs) {
      timestamps.pollFirst();
    }
    if (timestamps.size() >= limit) return false;
    timestamps.addLast(now);
    return true;
  }
}`},{type:"markdown",value:`### Sliding Window Counter

Combines fixed windows with a weighted blend of previous + current counts. If you are 75% into the current window:

\`weight = 0.25 \xD7 lastWindowRequests + currentWindowRequests\`

Admit if \`weight + 1 \u2264 limit\`. More accurate than fixed window, cheaper than the log.`},{type:"image",src:"assets/article-images/rate-limiter/10-sliding-window-counter.png",alt:"Sliding window counter weighting previous and current fixed windows",caption:"Weighted previous + current windows approximate a sliding window in O(1). Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"math",display:!0,tex:"count \\approx c_{cur} + c_{prev} \\cdot \\frac{(window - elapsed_{cur})}{window}",caption:"Weight the previous window by the fraction still inside the rolling window."},{type:"prosCons",title:"Sliding Window Counter",pros:["More accurate than fixed window","O(1) memory vs sliding log","Smooths window edges"],cons:["Slightly more complex to implement","Approximate, not exact"]},{type:"image",src:"assets/article-images/rate-limiter/11-sliding-window-counter-code.png",alt:"Code sketch of a sliding window counter",caption:"Reference implementation sketch. Diagram adapted from Ashish Pratap Singh / AlgoMaster."},{type:"code",language:"java",filename:"SlidingWindowCounter.java",code:`public final class SlidingWindowCounter {
  private final int limit;
  private final long windowMs;
  private long windowStart;
  private int prevCount;
  private int currCount;

  public SlidingWindowCounter(int limit, Duration window) {
    this.limit = limit;
    this.windowMs = window.toMillis();
    this.windowStart = System.currentTimeMillis();
  }

  public synchronized boolean tryAcquire() {
    long now = System.currentTimeMillis();
    long elapsed = now - windowStart;
    if (elapsed >= windowMs) {
      prevCount = (elapsed >= 2 * windowMs) ? 0 : currCount;
      currCount = 0;
      windowStart = now;
      elapsed = 0;
    }
    double weight = prevCount * (1.0 - (double) elapsed / windowMs) + currCount;
    if (weight + 1 > limit) return false;
    currCount++;
    return true;
  }
}`},{type:"callout",variant:"tip",title:"Production hygiene",body:"Communicate limits via `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `Retry-After`. For multi-node deployments use Redis/Lua (see Distributed section). Prefer token bucket or sliding window counter for most APIs."},{type:"callout",variant:"note",title:"Source",body:"Algorithm diagrams and trade-offs summarized from Ashish Pratap Singh\u2019s AlgoMaster article \u201CRate Limiting Algorithms Explained with Code,\u201D expanded with Java sketches for this platform."}]},{id:"high-level-architecture",title:"High-Level Architecture",blocks:[{type:"markdown",value:"Rate-limit middleware runs in each gateway node. On each request it **identifies** the caller (API key / user / IP), looks up the matching **rule**, and runs the algorithm against a **shared counter store** (Redis) using an atomic Lua script. A per-node **local cache** of tokens can absorb most checks to avoid a Redis round-trip on every request."},{type:"mermaid",caption:"Request path through the limiter.",definition:`flowchart TD
  Req["Incoming request"] --> Id["Identify caller\\n(API key / user / IP)"]
  Id --> Rule["Resolve rule\\n(limit, window, tier)"]
  Rule --> Local{Local token\\ncache hit?}
  Local -- yes, tokens left --> Allow["Allow \u2192 forward upstream"]
  Local -- no / empty --> Redis["Atomic check in Redis (Lua)"]
  Redis -- allowed --> Allow
  Redis -- over limit --> Reject["Reject: 429 + Retry-After"]
  Config[("Rules config")] -.-> Rule`},{type:"architectureCard",title:"Rate-Limit Middleware",description:"Embedded in the gateway. Extracts the client identity, resolves the applicable rule, executes the algorithm against the counter store, sets rate-limit response headers, and forwards or rejects.",icon:"shield",tags:["gateway","hot-path","headers"]},{type:"architectureCard",title:"Counter Store (Redis)",description:"Centralized, fast, in-memory counters keyed by identity+window. Runs the algorithm as an atomic Lua script so concurrent gateway nodes share one consistent view. Keys carry TTLs so memory self-cleans.",icon:"database",tags:["redis","atomic","ttl"]},{type:"architectureCard",title:"Rules Engine",description:"Hot-reloadable configuration mapping scopes (key/user/IP/endpoint) and tiers (free/paid) to limits and windows. Lets product change quotas without redeploying gateways.",icon:"settings",tags:["config","tiers","dynamic"]}]},{id:"distributed",title:"The Distributed Problem",blocks:[{type:"markdown",value:"With many gateway nodes, a per-node in-memory counter does not enforce a **global** limit \u2014 N nodes each allowing the limit means up to N\xD7 over-admission. The options trade accuracy against latency and the load they put on the shared store."},{type:"prosCons",title:"Centralized Redis vs local-with-sync",pros:["Centralized counter: globally accurate; one source of truth for all nodes.","Local counter + async sync: lowest latency; survives Redis blips; far less load on Redis.","Local with periodic sync can divide the quota across nodes for approximate global limits."],cons:["Centralized: a Redis round-trip on every request adds latency and a dependency.","Centralized: Redis is a hotspot/SPOF \u2014 must be replicated and sharded.","Local: temporarily over-admits (eventual consistency) until the next sync."]},{type:"callout",variant:"tip",title:"Common production pattern",body:'Use **centralized Redis** for correctness, but cushion it: a small **per-node local cache** answers the common "obviously under limit" case without a round-trip, and only consults Redis when near the boundary. This keeps p99 latency low while keeping global enforcement approximately correct.'},{type:"callout",variant:"warning",title:"Shard the counter store",body:"At high QPS a single Redis becomes the bottleneck. **Shard by client key** (consistent hashing) so each client's counter lives on one node \u2014 preserving atomicity per key while spreading load. Replicate each shard for availability."}]},{id:"api-design",title:"API & Response Contract",blocks:[{type:"markdown",value:"Rate limiting is mostly transparent, but the **rejection contract** matters: clients need to know they were limited and when to retry. Standard headers communicate the quota state on every response."},{type:"table",caption:"Standard rate-limit response headers.",headers:["Header","Meaning"],rows:[["HTTP 429 Too Many Requests","Status returned when the limit is exceeded"],["Retry-After","Seconds to wait before retrying"],["X-RateLimit-Limit","Max requests allowed in the window"],["X-RateLimit-Remaining","Requests left in the current window"],["X-RateLimit-Reset","When the window resets (epoch seconds)"]]},{type:"apiTable",title:"Rule management API (admin)",endpoints:[{method:"GET",path:"/admin/limits/{scope}",description:"Get the rule for a scope",auth:!0},{method:"PUT",path:"/admin/limits/{scope}",description:"Create/update a limit rule",auth:!0},{method:"DELETE",path:"/admin/limits/{scope}",description:"Remove a rule",auth:!0}]},{type:"callout",variant:"tip",title:"Reject cheaply, fail open",body:"A 429 should be returned **before** doing any expensive work. And decide your failure mode: if the counter store is unreachable, do you **fail open** (allow traffic, prioritize availability) or **fail closed** (reject, prioritize protection)? Most APIs **fail open** so a Redis outage does not take down the whole API \u2014 but a payment endpoint might fail closed."}]},{id:"scaling-strategy",title:"Scaling Strategy",blocks:[{type:"bestPractices",practices:["**Stateless gateways** scale horizontally; the shared counter holds the state.","**Shard Redis by client key** (consistent hashing) to spread counter load.","**Local token cache** per node to cut the common-case round-trip.","**Pipeline / batch** Redis ops where possible to amortize network cost.","**TTL on every key** so expired windows self-evict and memory stays bounded.","**Tiered + per-endpoint rules** so expensive endpoints get tighter limits."]},{type:"math",display:!0,tex:"Mem \\approx ActiveClients \\times KeysPerClient \\times BytesPerKey",caption:"Memory is driven by the number of distinct active client keys, not total request volume \u2014 TTLs keep only active windows resident."}]},{id:"availability",title:"Availability",blocks:[{type:"markdown",value:"The limiter is in the path of every request, so its availability is the API's availability. The store must be replicated, and the gateway must have a clear, safe behavior when the store is degraded."},{type:"callout",variant:"summary",title:"Fail open by default",body:"If Redis is unreachable, defaulting to **allow** (fail open) keeps the API serving traffic \u2014 a brief loss of rate limiting is usually better than a full outage. Combine with a **local fallback limiter** (approximate, per-node) so you are not completely unprotected during a store outage."},{type:"youtube",videoId:"mNwL2vNvUYM",title:"Designing a rate limiter (illustrative embed)"}]},{id:"consistency",title:"Consistency",blocks:[{type:"markdown",value:"Rate limiting is one of the cleaner examples of choosing **availability and latency over strict accuracy**. A globally perfect count is rarely worth the cost; admitting a handful of extra requests at a window edge is harmless, while adding 5ms to every request or risking an outage is not."},{type:"callout",variant:"note",title:"How much slack is acceptable?",body:'Define the tolerance explicitly. For abuse prevention, "100/min \xB1 a few" is fine \u2014 use local caches and async sync. For hard quotas with billing implications, tighten toward a centralized atomic counter. The algorithm and topology follow from this number.'}]},{id:"fault-tolerance",title:"Fault Tolerance",blocks:[{type:"bestPractices",practices:["**Replicate the counter store** (Redis primary + replicas) so a node loss does not disable limiting.","**Shard** so one hot client cannot overwhelm the whole store.","**Fail open** (or a local fallback limiter) when the store is unreachable.","**Atomic Lua scripts** to eliminate read-modify-write races under concurrency.","**TTLs everywhere** so crashes never leak stale counters.","**Circuit-break the Redis call** with a tight timeout so a slow store does not stall requests."]},{type:"expandable",title:"Example: fail-open wrapper around the check",blocks:[{type:"code",language:"python",filename:"check_with_failopen.py",code:`def allow(identity, rule):
    try:
        # Tight timeout: the limiter must never add real latency.
        return redis_lua_check(identity, rule, timeout_ms=5)
    except (RedisTimeout, RedisDown):
        # Store degraded \u2192 fail open (allow), optionally fall back to
        # an approximate per-node local limiter.
        metrics.incr("ratelimit.failopen")
        return local_fallback.allow(identity, rule)`}]}]},{id:"trade-offs",title:"Trade-offs",blocks:[{type:"table",caption:"Key decisions and what they cost.",headers:["Decision","Gain","Cost"],rows:[["Token bucket","O(1), allows bursts, intuitive","Burst can briefly exceed steady rate"],["Sliding window counter","No boundary spike, O(1)","Slight approximation"],["Sliding window log","Exact","O(n) memory per client"],["Centralized Redis","Globally accurate","Per-request latency + SPOF risk"],["Local cache + sync","Low latency, resilient","Temporary over-admission"],["Fail open","API stays up if store dies","Unprotected during outage"]]}]},{id:"technology-choices",title:"Technology Choices",blocks:[{type:"table",headers:["Concern","Real-world example"],rows:[["Counter store","Redis (in-memory, atomic Lua)"],["Atomicity","Redis Lua scripts / INCR+EXPIRE"],["Gateway enforcement","Envoy, NGINX, Kong, AWS API Gateway"],["Global rate-limit service","Envoy + ratelimit (Lyft) gRPC service"],["Sharding","Redis Cluster / consistent hashing"],["Config","Dynamic rules (hot-reload)"],["Algorithm","Token bucket / sliding window counter"]]},{type:"callout",variant:"note",title:"You usually do not build this from scratch",body:"Envoy's global rate-limit service, Kong/NGINX plugins, and cloud API gateways implement these algorithms for you. The value of this design is understanding *what they do* so you can configure them correctly and reason about their accuracy/latency trade-offs."}]},{id:"interview-questions",title:"Interview Questions",blocks:[{type:"sketchnote",title:"Rate Limiter Production Interview Board",intro:"A good answer connects algorithm behavior, distributed atomicity, failure policy, HTTP semantics, cost, monitoring, and testing.",items:[{code:"RL-11",glyph:"\u23F1",title:"What and why?",subtitle:"Bound usage over time",points:["Protects dependencies from abuse, bugs, retries, and traffic spikes","Enforces fairness, tenant plans, and expensive-API quotas","Rejects cheaply before consuming service/database capacity"],tip:"Rate limits are client quotas; load shedding protects total system capacity."},{code:"RL-12",glyph:"4\xD7",title:"Algorithms",subtitle:"Choose by desired traffic shape",points:["Fixed window: cheap, but has boundary bursts","Sliding log/counter: exact vs approximate rolling behavior","Token bucket: controlled bursts; leaky bucket: smooth output"],tip:"Name time, memory, burst, and accuracy trade-offs\u2014not just algorithms."},{code:"RL-13",glyph:"TB",title:"Token bucket wins often",subtitle:"O(1) state + natural bursts",points:["Capacity controls maximum burst","Refill rate controls sustained throughput","Avoids fixed-window\u2019s abrupt reset and near-2\xD7 boundary spike"],tip:"\u201CPreferred\u201D depends on product semantics; strict rolling quotas may favor sliding windows."},{code:"RL-14",glyph:"\u{1FA99}",title:"Real-world bucket",subtitle:"API gets spendable permits",points:["Bucket capacity 20; refill 10 tokens/second","Idle client accumulates 20 and may burst 20 requests","After depletion, only about 10 requests/second continue"],tip:"Expensive endpoints can consume multiple tokens."},{code:"RL-15",glyph:"R",title:"Redis distribution",subtitle:"One shared view across gateway nodes",points:["Store tokens/counters by tenant + route with TTL","Lua atomically reads, refills, decides, decrements, and writes","Cluster/shard by key and replicate for availability"],tip:"A Redis GET followed by SET is still a race."},{code:"RL-16",glyph:"!",title:"Redis unavailable",subtitle:"Choose policy before the incident",points:["Fail open for availability, or closed for sensitive protection","Use a tight timeout/circuit breaker; never stall every request","Fallback to a conservative per-node limiter and alert"],tip:"Policy can differ by route: public reads vs payments/login abuse."},{code:"RL-17",glyph:"Tier",title:"Free vs premium",subtitle:"Resolve dynamic policy by identity",points:["Map verified API key/user/tenant plan to rate, burst, and endpoint cost","Keep rules hot-reloadable, versioned, cached, and auditable","Apply safe defaults if plan lookup fails"],tip:"Do not trust a caller-supplied \u201Cpremium=true\u201D claim."},{code:"RL-18",glyph:"\u2195",title:"Gateway or service?",subtitle:"Usually both, for different scopes",points:["Gateway/CDN blocks abuse early with user/IP/API-key quotas","Service enforces domain limits requiring business state","Avoid duplicate inconsistent policies; define ownership centrally"],tip:"Client-side limiting improves UX but is never trusted enforcement."},{code:"RL-19",glyph:"429",title:"HTTP contract",subtitle:"Too Many Requests",points:["Return 429 before expensive upstream work","Include Retry-After plus limit, remaining, and reset metadata","Clients back off with jitter instead of retrying immediately"],tip:"403 means forbidden; 429 means temporarily over quota."},{code:"RL-20",glyph:"CAS",title:"Avoid race conditions",subtitle:"Make the whole state transition atomic",points:["Concurrent requests must not perform separate read-then-write steps","Use Redis Lua, atomic INCR+expiry pattern, or transactional CAS","Keep one key on one shard; define clock source consistently"],tip:"Atomic counters alone are insufficient for multi-step token refill logic."},{code:"RL-21",glyph:"$",title:"Reduce infrastructure cost",subtitle:"Reject waste at the cheapest layer",points:["Prevents excess requests from consuming CPU, DB connections, and paid APIs","Controls scraping, retry storms, and accidental tenant loops","Weighted quotas align expensive operations with customer plans"],tip:"Measure rejected downstream work to quantify savings."},{code:"RL-22",glyph:"M",title:"Production metrics",subtitle:"Observe outcomes and limiter health",points:["Allowed/rejected/fail-open counts by route and tier\u2014not raw user ID","Limiter latency, Redis errors, script time, saturation, and hot keys","429 ratio, remaining-quota distribution, and downstream load correlation"],tip:"Avoid high-cardinality user labels in Prometheus; use logs/traces for sampled identity detail."},{code:"RL-23",glyph:"Arch",title:"Production architecture",subtitle:"Edge middleware + shared atomic state",points:["Gateway authenticates, resolves route/tier rule, then runs Redis Lua","Allowed requests continue; rejected requests return 429 immediately","Local fallback, replicated Redis, config service, metrics, and audit complete it"],tip:"Describe only experience you actually had; otherwise present this explicitly as a proposed design."},{code:"RL-24",glyph:"Test",title:"How to test",subtitle:"Time, concurrency, distribution, and failure",points:["Use a fake clock for refill/window boundaries\u2014never sleep in unit tests","Fire concurrent requests across many app nodes and assert global admission","Test bursts, tiers, expiry, 429 headers, Redis latency/down, and recovery"],tip:"Shadow mode shows which real requests a new policy would reject before enforcement."},{code:"RL-25",glyph:"\u26A0",title:"Common challenges",subtitle:"The algorithm is the easy part",points:["Identity/cardinality, NAT/shared IPs, clock skew, and hot tenants","Accuracy vs latency, Redis failover, replica lag, and global regions","Rule rollout mistakes, unfair bursts, observability cost, and client retries"],tip:"State the accepted over-admission error and failure policy explicitly."}]},{type:"interviewQa",items:[{question:"Compare token bucket and leaky bucket.",answer:"Both are O(1). **Token bucket** allows **bursts** up to the bucket capacity, then limits to the refill rate \u2014 good for APIs that should tolerate short spikes. **Leaky bucket** processes requests at a fixed rate (a queue draining at constant speed), **smoothing** output and disallowing bursts \u2014 good for shaping a steady load onto a fragile downstream."},{question:"What is the fixed-window boundary problem and how do you fix it?",answer:"A fixed window resets its counter abruptly, so a client can send the full limit at the end of one window and again at the start of the next \u2014 up to **2\xD7 the limit in a short span**. Fix it with a **sliding window log** (exact, O(n) memory) or, more practically, a **sliding window counter** that weights the previous window (near-exact, O(1))."},{question:"How do you make a rate limiter work across many servers?",answer:"Keep counters in a **shared, fast store (Redis)** and update them with an **atomic Lua script** so all gateway nodes share one consistent count. Shard by client key for scale, replicate for availability, and add a per-node **local cache** + async sync to cut latency, accepting slight over-admission."},{question:"Why is atomicity critical, and how do you achieve it?",answer:'A non-atomic read-then-write lets two concurrent requests both read "1 token left" and both proceed \u2014 over-admitting. Redis executes a **Lua script as a single atomic operation** (Redis is single-threaded), making read-refill-decrement-write indivisible. Alternatively `INCR` returns the post-increment value atomically for window counters.'},{question:"What happens if the counter store goes down?",answer:"Decide the failure mode up front. **Fail open** (allow traffic) keeps the API available at the cost of temporarily losing limiting \u2014 the usual choice. **Fail closed** (reject) prioritizes protection \u2014 for sensitive endpoints. A per-node **local fallback limiter** gives approximate protection during the outage. Use a tight timeout so a slow store never stalls requests."},{question:"How do you support tiered limits (free vs paid)?",answer:"Resolve the client's **tier** from the API key during identification, then look up the matching rule (limit/window) from a **hot-reloadable rules config**. The same algorithm runs with different parameters per tier/scope/endpoint, so quotas change without redeploying the gateway."},{question:"How do you rate limit fairly when many different clients share one gateway node?",answer:"Key every counter on the **client identity** (API key/user/IP), never on the node \u2014 the counter store is shared across all gateway instances, so a client sees one consistent quota no matter which node handles the request. Per-node counters would let a client get N\xD7 the limit by spreading requests across N gateway instances."},{question:"Why not just use a database with a transaction for the counter instead of Redis?",answer:"A relational DB transaction (row lock, commit) adds several milliseconds and does not scale to the request-per-request hot path of a rate limiter. Redis is in-memory, supports atomic Lua scripts, and is purpose-built for exactly this pattern \u2014 sub-millisecond reads and writes at very high throughput, which a rate limiter's hot path demands."},{question:"How would you rate limit at multiple scopes simultaneously (e.g. per-user AND per-endpoint AND global)?",answer:"Run the algorithm against multiple keys per request (e.g. `user:42:endpoint:/search` and `global:endpoint:/search`) and reject if **any** scope is exceeded. Keep each scope as an independent counter with its own limit/window; a single Lua script can check several keys atomically in one round-trip to avoid N separate network calls."},{question:"How do you test/validate a rate limiter before shipping a change to the rules?",answer:"Run it in **shadow/dry-run mode** first: evaluate the new rule and log what *would* have happened (allow/reject) without actually enforcing it, compare against real traffic patterns, then flip it live. This catches misconfigured limits (e.g. too tight, breaking a legitimate integration) before they cause an outage."},{question:"How would you implement sliding window counter in Redis?",answer:"Store two counters: current window and previous window (e.g. keys keyed by `floor(now / window)`). On each request, `INCR` the current key (with TTL \u2248 2\xD7 window) and approximate usage as `prevCount \xD7 (1 - elapsedInWindow/window) + currCount`. Do the read\u2013weight\u2013decide\u2013incr in a **Lua script** so it stays atomic across gateway nodes."},{question:"How does a local token cache cause over-admission?",answer:"Each gateway node may cache a slice of tokens and admit locally without a Redis round-trip. Under concurrent load, nodes can collectively admit more than the global limit because their local views are stale until the next sync. You trade strict accuracy for latency \u2014 keep the cache small and sync frequently, or reserve local caching for soft limits only."},{question:"Difference between rate limiting and throttling?",answer:"**Rate limiting** is a hard quota: exceed it and the request is **rejected** (often 429). **Throttling** **slows** excess traffic (delay, queue, or degrade) so some work still completes under load. Many gateways combine both \u2014 reject clear abuse, throttle borderline bursts."}]}]},{id:"references",title:"References",blocks:[{type:"references",items:[{label:"Stripe: Scaling your API with rate limiters",url:"https://stripe.com/blog/rate-limiters",source:"Stripe"},{label:"Envoy global rate limiting",url:"https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/other_features/global_rate_limiting",source:"Envoy"},{label:"Cloudflare: How we built rate limiting",url:"https://blog.cloudflare.com/counting-things-a-lot-of-different-things/",source:"Cloudflare"},{label:"Redis rate limiting patterns (INCR)",url:"https://redis.io/learn/howtos/ratelimiting",source:"Redis"},{label:"System Design Interview \u2014 Rate Limiter (Alex Xu)",url:"https://bytebytego.com/courses/system-design-interview/design-a-rate-limiter",source:"ByteByteGo"}]}]},{id:"summary",title:"Summary",blocks:[{type:"callout",variant:"summary",title:"Key takeaways",body:`1. **Enforce at the gateway** \u2014 it is centralized, trustworthy, and keeps limiting out of every service.
2. **Pick the algorithm for the burst behavior you want**: token bucket (bursty, O(1)) or sliding window counter (smooth, O(1), no boundary spike); leaky bucket to shape a steady output.
3. **Atomicity is the core correctness detail**: an atomic Redis Lua script (or \`INCR\`) eliminates read-modify-write races across nodes.
4. **Distribution is a trade-off**: centralized Redis for accuracy, local caches + async sync for latency \u2014 choose based on how much over-admission you can tolerate.
5. **Plan for failure**: replicate and shard the store, set tight timeouts, and decide fail-open vs fail-closed so the limiter never becomes the outage.`}]}]},a=t;export{a as default};
